enum Method { GET, POST, DELETE, PATCH, UPDATE, PUT }

enum PasswordFieldType { CURRENT, NEW, CONFIRM }

enum MenuAvailability { AVAILABLE, UNAVAILABLE, OUT_OF_STOCK }

enum Env { DEV, STAGE, LIVE }

enum DateType { today, yesterday, range }

enum CheckoutState { PAY_NOW, PLACE_ORDER }

enum PaymentState { PRE_PAYMENT, POST_PAYMENT }

enum FilterOption { singleDate, singleBranch, multipleBrand, multipleBranch, multipleProvider }
